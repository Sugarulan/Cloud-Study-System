/**
 * 评卷模块边界（Spring Modulith 风格）。
 *
 * <p>本包内的类只允许通过 {@code EvaluationController} / 事件 与外部通信。</p>
 */
@org.springframework.modulith.ApplicationModule(displayName = "评卷模块")
package com.gac.lms.module.evaluation;
