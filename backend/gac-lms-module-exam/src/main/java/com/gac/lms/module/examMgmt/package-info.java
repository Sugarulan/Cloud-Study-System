/**
 * 考试管理模块边界。
 *
 * <p>负责考试创建/编辑/发布、交卷规则、参考范围、考试周期管理。W2 阶段实现业务。</p>
 *
 * <p>注意：本模块的 Maven 坐标为 {@code gac-lms-module-exam}，Java 包为 {@code com.gac.lms.module.examMgmt}，
 * 是为了避免与 {@code gac-lms-module-exam-taking}（在线作答）所占用的 {@code com.gac.lms.module.exam} 包冲突。</p>
 */
package com.gac.lms.module.examMgmt;
