<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.gac.lms</groupId>
        <artifactId>gac-lms-backend</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>gac-lms-module-person</artifactId>
    <name>gac-lms-module-person</name>
    <description>3.3.2 人员信息管理：人员创建/更新、账号关联、分组、导入导出</description>

    <dependencies>
        <dependency>
            <groupId>com.gac.lms</groupId>
            <artifactId>gac-lms-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.gac.lms</groupId>
            <artifactId>gac-lms-infrastructure</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!--
          创建人员时可选同时创建账号，复用 AccountService。
          单向依赖（person → account），不会循环。
        -->
        <dependency>
            <groupId>com.gac.lms</groupId>
            <artifactId>gac-lms-module-account</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
</project>
